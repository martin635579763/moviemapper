import { Minus } from 'lucide-react';
import type { LucideProps } from 'lucide-react';

// Using Minus as a simple representation for an aisle path
export const AisleIcon = (props: LucideProps) => <Minus {...props} />;
